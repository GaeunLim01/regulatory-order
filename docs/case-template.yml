# Framework template — current analytical frame
# Case-specific content is intentionally open while the case library is being rebuilt.

id:
title:
status: draft

# Shared analytical lenses. A case may use each lens at a different depth.
ordering:
  summary:
  markers: []

manifestation:
  observations: []

distribution:
  source:
  bearer:
  mechanisms: []

# Optional case-level characterization, only when analytically useful.
# This is an output of analysis, not a required event taxonomy.
case_character:
